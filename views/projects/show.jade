extends ../layout

block content

  include includes/about

  if tutorials.length
    section
      h2 #{project.name} tutorials:
      include ../tutorials/includes/list
  else
    h2 No tutorials. 
    p 
      a(href='/tutorial/new?project=' + project.platform + '/' + project.name) Create tutorial for #{project.name}
